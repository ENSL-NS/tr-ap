/// <reference types="node" />
import { EventEmitter } from 'events';
export declare const isUsbBootCapableUSBDevice: (idVendor: number, idProduct: number) => boolean;
export declare const isROMUSBDevice: (idVendor: number, idProduct: number) => boolean;
export declare const isSPLUSBDevice: (idVendor: number, idProduct: number) => boolean;
export declare class UsbBBbootScanner extends EventEmitter {
    private usbBBbootDevices;
    private boundAttachDevice;
    private boundDetachDevice;
    private interval;
    private attachedDeviceIds;
    constructor();
    start(): void;
    stop(): void;
    private step;
    private incrementStep;
    private get;
    private getOrCreate;
    private remove;
    private attachDevice;
    private process;
    private transfer;
    private closeDevice;
    private detachDevice;
}
export declare class UsbBBbootDevice extends EventEmitter {
    portId: string;
    static readonly LAST_STEP = 1124;
    private _step;
    constructor(portId: string);
    get progress(): number;
    get step(): number;
    set step(step: number);
}
